// profile-service.js

const User = require('../schemas/user');

class ProfileService {
  static async createProfile(userId, profileData) {
    // 프로필 생성 로직
  }

  static async editProfile(userId, newProfileData) {
    // 프로필 편집 로직
  }
}

module.exports = ProfileService;
